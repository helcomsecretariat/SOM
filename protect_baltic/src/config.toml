# Configuration file

# export path
export_path = './data/results.xlsx'

# scenario used for calculations
use_scenario = false
scenario = 'ChangeML'

# randomness setup
use_random_seed = true
random_seed = 10022025

# number of simulations
simulations = 10

# excel input files
use_legacy_input_data = false
[input_data]
    path = './data/input_data.xlsx'
[input_data_legacy]
    general_input = './data/generalInput.xlsx'
    measure_effect_input = './data/measureEffectInput.xlsx'
    pressure_state_input = './data/pressureStateInput.xlsx'
    [input_data_legacy.general_input_sheets]
        case = 'ActMeas'
        postprocess = 'ActPres'
        overlaps = 'Overlaps'
        development_scenarios = 'DEV_scenarios'
        subpressures = 'SubPres'
        [input_data_legacy.general_input_sheets.ID]
            measure = 'Measure ID'
            activity = 'Activity ID'
            pressure = 'Pressure ID'
            state = 'State ID'
            area = 'Area ID'
